# API keys are read from the environment only; never commit a real .env
# Copy this file to .env and fill in your keys (.env is gitignored)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
# Optional: default model selection (gpt-5 | claude); overridden by --model
COPPERHEAD_MODEL=
